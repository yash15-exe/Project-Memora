<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        header {
            background-color: #333;
            color: #fff;
            padding: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        #logo {
            width: 80px;
            height: auto;
        }

        nav {
            display: flex;
            gap: 20px;
        }

        nav a {
            text-decoration: none;
            color: #fff;
            font-weight: bold;
            padding: 8px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        nav a:hover {
            background-color: #555;
        }

        .login-signup {
            display: flex;
            gap: 10px;
        }

        .login-signup button {
            padding: 8px;
            border-radius: 5px;
            background-color: #5cb85c;
            color: #fff;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .login-signup button:hover {
            background-color: #4cae4c;
        }
    </style>
</head>

<body>
    <header>
        <img id="logo" src="your-logo.png" alt="Event Planner Logo">
        <nav>
            <a href="#">Home</a>
            <a href="#">Services</a>
            <a href="#">Portfolio</a>
            <a href="#">Contact</a>
        </nav>
        <div class="login-signup">
            <button>Login</button>
            <button>Sign Up</button>
        </div>
    </header>

    <!-- Your website content goes here -->

</body>

</html>
